Default profile: tafdg_gtsrb_stable
This build rewrites client-side alignment to operate on clean local deltas, uses practical round-aware DP by default, and normalizes DP noise by selected dimension to avoid high-dimensional utility collapse.
